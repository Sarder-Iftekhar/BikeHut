namespace BikeShop.Helpers
{
    public class Roles
    {
        //its helper class where we define roles as constant.and we should use those 
        //constant instead of directly using hardcoded value in applicable classes
        public const string Admin = "Admin";
        public const string Executive = "Executive";
    }
}
